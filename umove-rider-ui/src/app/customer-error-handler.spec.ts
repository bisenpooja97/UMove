import { CustomerErrorHandler } from './customer-error-handler';

describe('CustomerErrorHandler', () => {
  it('should create an instance', () => {
    expect(new CustomerErrorHandler()).toBeTruthy();
  });
});
