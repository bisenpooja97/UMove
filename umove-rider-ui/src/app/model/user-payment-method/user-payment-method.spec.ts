import { UserPaymentMethod } from './user-payment-method';

describe('UserPaymentMethod', () => {
  it('should create an instance', () => {
    expect(new UserPaymentMethod()).toBeTruthy();
  });
});
